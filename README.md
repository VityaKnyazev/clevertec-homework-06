<h1>CLEVERTEC (homework-04)</h1>

<p>CLEVERTEC homework-04:</p>
<p>Склонировать свой репо с тестовым заданием, сделать отдельную ветку, туда перенести уже 
существующие тесты, после покрыть уже существующий код минимум на 70%(не включая entity и dto).</p>
<p>В тестах я ожидаю увидеть Mock’s, ArgumentCaptor, Parametrised Tests. Если будете править код, 
то делать это не в ветке с тестами. После завершения, сделать pull request на dev, и скинуть на гугл форму ниже.</p>

<h2>Что сделано:</h2>
<ol>
<li>Склонирован мой репо с тестовым заданием, сделана отдельная ветка test.</li>
<li>В ветку test перенесено все из ветки develop (в том числе тесты).</li>
<li>Произведен рефакторинг тестов в соответствии с заданием. Существующий код покрыт на ~ 70 %.</li>
	<ol>
		<p>В тестах добавлено:</p>
		<li>Построение тестовых объектов (entity) с помощью паттерна Builder, используя библиотеку lombok.</li>
		<li>В отдельных тестах использован декларативный подход при создании моков и их добавлении в
		тестируемый класс с помощью аннотаций @Mock, @InjectMocks, @ExtendWith.</li>
		<li>Именование методов исправлено на checkMethodName.*Should.*.</li>
		<li>Использование методов библиотеки AssertJ в тестах и статический импорт ее методов.</li>
		<li>Nested class в StorageServiceImplTest классе</li>
		<li>Mockito.verify, а также ArgumentCaptor для тестирования void методов.</li>
		<li>Параметризированные тесты.</li>
	</ol>
<li>Из ветки test все что под src/main/java, src/main/resources и docker-compose.yml не добавлялось в последний комит для удобства. </li>
<li>Создан pull request на develop.</li>
</ol>

<h3>Стек</h3>
<p>Java 8, Gradle 7.5, lombok. Test: Junit 5 + Mockito, AssertJ</p>